const Role = {
  ADMINISTRATOR: 'ADMINISTRATOR',
  REGISTERED: 'REGISTERED',
  GUEST: 'GUEST',
  NONE: 'NONE'
};

const Status = {
  PENDING: 'PENDING',
  APPROVED: 'APPROVED'
}

const MessageType = {
  MESSAGE: 'MESSAGE',
  NOTIFICATION: 'NOTIFICATION'
}

export = {
  Role,
  Status,
  MessageType
}